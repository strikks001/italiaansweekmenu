export default defineAppConfig({
  // Nuxt UI-standaarden. Wil je later een eigen huisstijl, dan definieer je
  // hier de semantische kleuren (primary, secondary, neutral) en registreer je
  // de bijbehorende paletten in app/assets/css/main.css.
  ui: {}
})
