export default defineAppConfig({
  // Palettes live in app/assets/css/main.css; here they get their role.
  ui: {
    colors: {
      primary: 'vermilion',
      secondary: 'ceramic',
      neutral: 'stone'
    }
  }
})
